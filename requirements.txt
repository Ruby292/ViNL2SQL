# Zero-shot Text-to-SQL Pipeline Dependencies

# Core inference
vllm>=0.4.0
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.27.0

# QLoRA fine-tuning stack
peft>=0.12.0
bitsandbytes>=0.43.0
datasets>=2.20.0
trl>=0.9.6

# Utilities
tqdm
nltk

# Contextual augmentation (opt-in via --augment)
underthesea>=6.8.0
sentence-transformers>=3.0.0

# Optional: for better performance
# ninja  # for faster compilation
# flash-attn>=2.0.0  # for flash attention support
